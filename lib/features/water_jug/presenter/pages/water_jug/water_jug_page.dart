import 'package:flutter/material.dart';

class WaterJugPage extends StatelessWidget {
  const WaterJugPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
